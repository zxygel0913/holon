{% comment %}

This snippet generates the structure of a comment.
It is used to show not only recent comments, but also comments that were
just published but not yet approved.

More information:
- http://docs.shopify.com/themes/liquid-variables/comment

{% endcomment %}

<div class="dt-sc-comments-section">
  <div class="dt-sc-comments-meta">
    <p class="dt-sc-blog-date">
      {% include 'icon-date' %} <time datetime="{{ comment.created_at | date: '%Y-%m-%dT%H:%M:%SZ' }}">{{ comment.created_at | date: format: 'date' }}</time>            
    </p>

    <p class="dt-sc-blog-author">
      {% include 'icon-author' %}
      {{ comment.author }}
    </p>
  </div>  

  <div class="dt-sc-comment-description rte">
    {{ comment.content }}
  </div>
</div>